__pycache__/
*.pyc
.venv/
venv/
.env
.streamlit/secrets.toml
**/.streamlit/secrets.toml
.DS_Store

# Assignment PDFs and the Lab 4 source syllabi stay local; the built vector DB
# is committed instead, so the deployed app never needs to read them.
*.pdf
Documents/
